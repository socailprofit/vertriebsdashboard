# Sales Pulse release, 09 September 2026

This release publishes the previously prepared Antony surface and records the already deployed persistent-process backend. It does not redesign the dashboard or change CRM records.

- Activity uses actual event dates; cohorts use the same documented sales processes. Future meetings remain planning.
- Persistent source revisions, replacements, acquisition deduplication and cross-month task/calendar links are retained.
- LinkedIn supplier attribution is separate from the employee who performed a conversation.
- Antony prioritizes the pipeline, documented next actions and cumulative graph. Historical outcomes and other open processes are disclosed separately. Calculator and AI remain available.

## Remaining sync correction

The 08:23 Berlin failure reached finalization after all 66 chunks. Four recent failed runs ended at approximately the 25-second upload deadline. The client can return an aborted request as an error object without a Postgres code; this was classified as `funnel_upload_rpc_failed` instead of a deadline. A regression test reproduces this classification error.

The uploader now identifies returned aborts explicitly. Its maximum budget increases from 25 to 45 seconds to accommodate upload/checkpoint latency plus the existing 20-second SQL finalizer budget. The overall 140-second cutoff, 145-second Cron request timeout, SQL transaction, idempotent run/chunk identities, authorization and schedule remain unchanged. No payloads or token claims are added to error logs. Edge function: close-sync version 95.

## Validation before frontend publication

- 215 Node tests: `node --experimental-transform-types --test tests/*.test.*`.
- Deno checks for close-sync, kpi-assistant and weekly-review.
- 30 persistent reporting SQL scenarios, including October continuation, completed reminders, moved CC2 calendar identity and service-only access.
- Live security regression: all synthetic fixtures rolled back. Existing Antony/info access allowed; both other accounts denied. No client grants to raw/history tables or internal/import RPCs; all history tables have RLS.
- Signups and anonymous sign-ins disabled; email confirmation enabled.
- Live aggregate report: 10 Setter calendar appointments, 8 Setter conversations, 3 Closer conversations, 3 CC2 agreements and 1 first customer in September to the current snapshot. Calendar supplier breakdown: 6 Michael, 4 LinkedIn. Four documented CC2 follow-up tasks include 8 and 14 October.
- Local UI: supplier filter, calculator and 390-pixel rendering verified. Live frontend and subsequent Cron verification follow publication.

The Supabase advisor still reports disabled leaked-password protection, and informational findings for intentionally inaccessible RLS tables plus warnings for explicitly guarded public SECURITY DEFINER entry points. No broader permissions were granted to remove advisor findings.

Cache identifier: `2026-09-09-antony-release`.
